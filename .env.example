# Copy to .env and fill in your local values. .env is gitignored — never commit it.

DB_HOST=localhost
DB_NAME=jobmarket
DB_USER=postgres
DB_PORT=5432
DB_PASSWORD=your_password_here

# Optional: a full connection string overrides the DB_* vars above.
# DATABASE_URL=postgresql://user:password@host:5432/dbname
